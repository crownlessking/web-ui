import * as fc from '../../../components/content/form.component'

describe('src/components/content/form.component.tsx', () => {
  it('should import without error', () => {
    expect(fc).toBeTruthy()
  })
})