import * as F from '../../../components/content/webapp.content.component'

describe('src/components/content/webapp.content.component.tsx', () => {
  it('should import without error', () => {
    expect(F).toBeTruthy()
  })
})