import * as F from '../../../components/pages/errors.component'

describe('src/components/pages/errors.component.tsx', () => {
  it('should import without error', () => {
    expect(F).toBeTruthy()
  })
})