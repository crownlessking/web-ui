import * as F from '../../../components/pages/landing.component'

describe('src/components/pages/landing.component.tsx', () => {
  it('should import without error', () => {
    expect(F).toBeTruthy()
  })
})
